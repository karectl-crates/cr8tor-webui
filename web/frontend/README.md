# Frontend (React)

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Run the development server:
   ```bash
   npm start
   ```

- The app will be available at http://localhost:3000
- Make sure the backend FastAPI server is running at http://localhost:8000
